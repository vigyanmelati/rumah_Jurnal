<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.vigyan.rumahjurnal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rumahjurnal">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TentangKamiActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".ViewPdfActivity" />
        <activity android:name=".DaftarJurnalActivity" />
        <activity android:name=".DaftarJurnalUserActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".CreateDataActivity" />
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".EditUserActivity" />
    </application>

</manifest>